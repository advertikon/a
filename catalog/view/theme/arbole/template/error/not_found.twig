<!doctype html>
<html>
  <head>
  <title>{{ __( 'Arbole' ) }}</title>
  {{ include( 'arbole/template/common/meta.twig' ) }}
  </head>
  <body class="e404">
    <div class="max-width">
      <div class="error-wrapper">
        <img src="{{ image( 'arbole/logo-404.png') }}"/>
        <p><strong>404</strong>{{ __( 'Oops. Something went wrong. You’re not sure what — was it you? Was it the website? What do you do now?' ) }}</p>
        <div class="btn-wrap">
          <a href="index.php"><span></span>{{ __( 'home page' ) }}</a>
        </div>
      </div>
    </div>
  </body>
</html>